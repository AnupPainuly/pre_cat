#include<stdio.h>
int main(void)
{
    char nl='\n',r='\r',t='\t',a='\a',b='\b';
    printf("ASCII values of newline %d,return carriage %d,tab %d,alarm %d,backspace %d",nl,r,t,a,b);
    return 0;
}