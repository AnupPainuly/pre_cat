#include<stdio.h>
int main(void)
{
    int a;
    printf("enter the number: ");
    scanf("%d",&a);
    printf(" the char value is %c, the decimal value is %d, the hex value is %x, the octal value is %o",a,a,a,a);
    return 0;

}