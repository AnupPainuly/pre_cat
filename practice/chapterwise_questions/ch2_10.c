#include<stdio.h>
int main(void)
{
    printf("%d",++4); //increment or decrement operators can be used only with expressions and not with constants.
                    //locator value error.  
    return 0;
}